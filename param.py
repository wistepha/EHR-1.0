iloop = 1
nloop = 1
trigadc = 100000

#ontest=1 terminates the prog right after it finished
#-->useful if output is written in a text file via: >>file.txt 2>&1 (or simply >file.txt)
#ontest=0 makes the program waiting for an input --> histo is visible
ontest = 0

#if == 1  prints only the results of the test. == 0 prints all nloops, events and results of each iloop
showres = 0
